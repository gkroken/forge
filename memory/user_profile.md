---
name: user-profile
description: Who the user is, their expertise, and how to tailor responses
metadata:
  type: user
---

Senior software engineer, Go-first. Works on a full-stack Go project (forge — artifact repo manager) with embedded HTML/CSS/JS templating. Comfortable with systems-level thinking: storage interfaces, JSON marshaling edge cases, backward-compat migration patterns.

Does not need explanations of basic Go patterns. Skip the "here's why this works" commentary — state the decision and move.

UI work is secondary skill for them: they make pragmatic choices (inline SVG charts, no charting lib) and don't overthink browser compat.

**How to apply:** Pitch explanations at "senior Go engineer who hasn't touched this specific subsystem yet." No hand-holding on stdlib, interfaces, or testing patterns. Do explain non-obvious project-specific invariants (like the nil=server-default pointer convention).
